package ${cfg.package_form};

import io.swagger.annotations.ApiModel;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.NoArgsConstructor;
import lombok.ToString;
import lombok.experimental.Accessors;
import lombok.experimental.SuperBuilder;

/**
 * <p>Description: $!{table.comment} 分页查询参数实体 (根据业务需求添加字段) </p>
 *
 * @author ${author}
 * @version ${cfg.version}
 * @email "mailto:${author}@gmail.com"
 * @date ${cfg.date}
 * @since ${cfg.version}
 */
@Data
@SuperBuilder
@NoArgsConstructor
@AllArgsConstructor
@Accessors(chain = true)
@ToString(callSuper = true)
@EqualsAndHashCode(callSuper = true)
@ApiModel(description = "分页查询条件")
public class ${entity}Query extends Query<Long> {
    /** serialVersionUID */
    private static final long serialVersionUID = 1L;
    /** todo: [自动生成的字段, 避免此实体没有字段导致启动失败的问题, 可删除] */
    private String autoField;
}
